#pragma once
#include "obj.h";

int main()
{
	yinux();



	//char str[] = "home";
	//char delims[] = "/";
	//char *result = NULL;
	//result = strtok(str, delims);
	//while (result != NULL) {
	//	printf("result is \"%s\"\n", result);
	//	result = strtok(NULL, delims);
	//}

	system("pause");
	return 0;
}